# Copy to .env (git-ignored) for `docker compose up --build`.
# Real CatBoost models + live Open-Meteo weather + OpenAI explanations.
MODEL_ADAPTER=real
WEATHER_PROVIDER=open_meteo
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
EXPLANATION_LANGUAGE=ru
