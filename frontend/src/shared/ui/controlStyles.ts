/** Borderless control that sits inside a `.field` box (the field draws the frame). */
export const controlBase =
  "h-7 w-full bg-transparent text-[15px] text-ink outline-none transition-colors disabled:opacity-60";
